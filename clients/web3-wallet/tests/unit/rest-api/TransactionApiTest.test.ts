/**
 * Binance Web3 Wallet REST API
 *
 * Cross-chain wallet, market, trading, and transaction APIs for the OnchainOS platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { jest, expect, beforeEach, describe, it } from '@jest/globals';
import { JSONParse, JSONStringify } from 'json-with-bigint';
import { ConfigurationRestAPI, type RestApiResponse } from '@binance/common';

import { TransactionApi } from '../../../src/rest-api';
import {
    BroadcastTransactionsRequest,
    GetBroadcastOrdersRequest,
    GetGasLimitRequest,
    GetGasPriceRequest,
    GetTransactionSupportedChainsRequest,
    SimulateTransactionsRequest,
} from '../../../src/rest-api';
import type {
    BroadcastTransactionsResponse,
    GetBroadcastOrdersResponse,
    GetGasLimitRequestEvmTx,
    GetGasLimitRequestSolTx,
    GetGasLimitResponse,
    GetGasPriceResponse,
    GetTransactionSupportedChainsResponse,
    SimulateTransactionsRequestEvmTx,
    SimulateTransactionsRequestSolTx,
    SimulateTransactionsResponse,
} from '../../../src/rest-api/types';

describe('TransactionApi', () => {
    let client: TransactionApi;
    let config: ConfigurationRestAPI;
    let mockResponse: object = {};

    beforeEach(() => {
        config = new ConfigurationRestAPI({
            apiKey: 'test-api-key',
            apiSecret: 'test-api-secret',
            basePath: '',
        });
        client = new TransactionApi(config);
    });

    describe('broadcastTransactions()', () => {
        it('should execute broadcastTransactions() successfully with required parameters only', async () => {
            const params: BroadcastTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                signedTransaction: 'signedTransaction_example',
                address: 'address_example',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        orderId: '1812345678901234567',
                        txHash: '0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890',
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'broadcastTransactions').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<BroadcastTransactionsResponse>)
            );
            const response = await client.broadcastTransactions(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute broadcastTransactions() successfully with optional parameters', async () => {
            const params: BroadcastTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                signedTransaction: 'signedTransaction_example',
                address: 'address_example',
                recvWindow: 5000,
                nonce: 'unique-nonce-string',
                enableMevProtection: true,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        orderId: '1812345678901234567',
                        txHash: '0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890',
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'broadcastTransactions').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<BroadcastTransactionsResponse>)
            );
            const response = await client.broadcastTransactions(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when binanceChainId is missing', async () => {
            const _params: BroadcastTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                signedTransaction: 'signedTransaction_example',
                address: 'address_example',
            };
            const params = Object.assign({ ..._params });
            delete params?.binanceChainId;

            await expect(client.broadcastTransactions(params)).rejects.toThrow(
                'Required parameter binanceChainId was null or undefined when calling broadcastTransactions.'
            );
        });

        it('should throw RequiredError when signedTransaction is missing', async () => {
            const _params: BroadcastTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                signedTransaction: 'signedTransaction_example',
                address: 'address_example',
            };
            const params = Object.assign({ ..._params });
            delete params?.signedTransaction;

            await expect(client.broadcastTransactions(params)).rejects.toThrow(
                'Required parameter signedTransaction was null or undefined when calling broadcastTransactions.'
            );
        });

        it('should throw RequiredError when address is missing', async () => {
            const _params: BroadcastTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                signedTransaction: 'signedTransaction_example',
                address: 'address_example',
            };
            const params = Object.assign({ ..._params });
            delete params?.address;

            await expect(client.broadcastTransactions(params)).rejects.toThrow(
                'Required parameter address was null or undefined when calling broadcastTransactions.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: BroadcastTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                signedTransaction: 'signedTransaction_example',
                address: 'address_example',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'broadcastTransactions')
                .mockRejectedValueOnce(mockError);
            await expect(client.broadcastTransactions(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('getBroadcastOrders()', () => {
        it('should execute getBroadcastOrders() successfully with required parameters only', async () => {
            const params: GetBroadcastOrdersRequest = {
                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                binanceChainId: '1',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        cursor: 'eyJ0aW1lIjoxNzQ4NTk5MDAwMDAwLCJpZCI6Nzh9',
                        orders: [
                            {
                                orderId: '1812345678901234567',
                                binanceChainId: '1',
                                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                                txHash: '0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890',
                                txStatus: 'success',
                                failReason: 'execution reverted: insufficient liquidity',
                            },
                        ],
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getBroadcastOrders').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetBroadcastOrdersResponse>)
            );
            const response = await client.getBroadcastOrders(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getBroadcastOrders() successfully with optional parameters', async () => {
            const params: GetBroadcastOrdersRequest = {
                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                binanceChainId: '1',
                recvWindow: 5000,
                nonce: 'unique-nonce-string',
                txStatus: 'success',
                orderId: '1812345678901234567',
                cursor: 'eyJ0aW1lIjoxNzQ4NjAwMDAwMDAwLCJpZCI6MTIzfQ==',
                limit: 20,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        cursor: 'eyJ0aW1lIjoxNzQ4NTk5MDAwMDAwLCJpZCI6Nzh9',
                        orders: [
                            {
                                orderId: '1812345678901234567',
                                binanceChainId: '1',
                                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                                txHash: '0xabc123def4567890abc123def4567890abc123def4567890abc123def4567890',
                                txStatus: 'success',
                                failReason: 'execution reverted: insufficient liquidity',
                            },
                        ],
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getBroadcastOrders').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetBroadcastOrdersResponse>)
            );
            const response = await client.getBroadcastOrders(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when address is missing', async () => {
            const _params: GetBroadcastOrdersRequest = {
                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                binanceChainId: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.address;

            await expect(client.getBroadcastOrders(params)).rejects.toThrow(
                'Required parameter address was null or undefined when calling getBroadcastOrders.'
            );
        });

        it('should throw RequiredError when binanceChainId is missing', async () => {
            const _params: GetBroadcastOrdersRequest = {
                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                binanceChainId: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.binanceChainId;

            await expect(client.getBroadcastOrders(params)).rejects.toThrow(
                'Required parameter binanceChainId was null or undefined when calling getBroadcastOrders.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: GetBroadcastOrdersRequest = {
                address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                binanceChainId: '1',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getBroadcastOrders').mockRejectedValueOnce(mockError);
            await expect(client.getBroadcastOrders(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('getGasLimit()', () => {
        it('should execute getGasLimit() successfully with required parameters only', async () => {
            const params: GetGasLimitRequest = {
                binanceChainId: 'binanceChainId_example',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: { gasLimit: '21000' },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getGasLimit').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetGasLimitResponse>)
            );
            const response = await client.getGasLimit(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getGasLimit() successfully with optional parameters', async () => {
            const params: GetGasLimitRequest = {
                binanceChainId: 'binanceChainId_example',
                recvWindow: 5000,
                nonce: 'unique-nonce-string',
                evmTx: {} as GetGasLimitRequestEvmTx,
                solTx: {} as GetGasLimitRequestSolTx,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: { gasLimit: '21000' },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getGasLimit').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetGasLimitResponse>)
            );
            const response = await client.getGasLimit(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when binanceChainId is missing', async () => {
            const _params: GetGasLimitRequest = {
                binanceChainId: 'binanceChainId_example',
            };
            const params = Object.assign({ ..._params });
            delete params?.binanceChainId;

            await expect(client.getGasLimit(params)).rejects.toThrow(
                'Required parameter binanceChainId was null or undefined when calling getGasLimit.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: GetGasLimitRequest = {
                binanceChainId: 'binanceChainId_example',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getGasLimit').mockRejectedValueOnce(mockError);
            await expect(client.getGasLimit(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('getGasPrice()', () => {
        it('should execute getGasPrice() successfully with required parameters only', async () => {
            const params: GetGasPriceRequest = {
                binanceChainId: '1',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        evmLegacyGasPrice: {
                            lowGasPrice: '1000000000',
                            mediumGasPrice: '2000000000',
                            highGasPrice: '5000000000',
                        },
                        eip1559GasPrice: {
                            baseFee: '1500000000',
                            lowPriorityFee: '100000000',
                            lowMaxFee: '1700000000',
                            mediumPriorityFee: '500000000',
                            mediumMaxFee: '2200000000',
                            highPriorityFee: '1000000000',
                            highMaxFee: '3000000000',
                        },
                        solanaGasPrice: {
                            lowComputeUnitPrice: '500',
                            lowJitoTip: '10000',
                            mediumComputeUnitPrice: '1000',
                            mediumJitoTip: '50000',
                            highComputeUnitPrice: '5000',
                            highJitoTip: '200000',
                        },
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getGasPrice').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetGasPriceResponse>)
            );
            const response = await client.getGasPrice(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getGasPrice() successfully with optional parameters', async () => {
            const params: GetGasPriceRequest = {
                binanceChainId: '1',
                recvWindow: 5000,
                nonce: 'unique-nonce-string',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        evmLegacyGasPrice: {
                            lowGasPrice: '1000000000',
                            mediumGasPrice: '2000000000',
                            highGasPrice: '5000000000',
                        },
                        eip1559GasPrice: {
                            baseFee: '1500000000',
                            lowPriorityFee: '100000000',
                            lowMaxFee: '1700000000',
                            mediumPriorityFee: '500000000',
                            mediumMaxFee: '2200000000',
                            highPriorityFee: '1000000000',
                            highMaxFee: '3000000000',
                        },
                        solanaGasPrice: {
                            lowComputeUnitPrice: '500',
                            lowJitoTip: '10000',
                            mediumComputeUnitPrice: '1000',
                            mediumJitoTip: '50000',
                            highComputeUnitPrice: '5000',
                            highJitoTip: '200000',
                        },
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getGasPrice').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetGasPriceResponse>)
            );
            const response = await client.getGasPrice(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when binanceChainId is missing', async () => {
            const _params: GetGasPriceRequest = {
                binanceChainId: '1',
            };
            const params = Object.assign({ ..._params });
            delete params?.binanceChainId;

            await expect(client.getGasPrice(params)).rejects.toThrow(
                'Required parameter binanceChainId was null or undefined when calling getGasPrice.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: GetGasPriceRequest = {
                binanceChainId: '1',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'getGasPrice').mockRejectedValueOnce(mockError);
            await expect(client.getGasPrice(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('getTransactionSupportedChains()', () => {
        it('should execute getTransactionSupportedChains() successfully with required parameters only', async () => {
            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: [
                        {
                            binanceChainId: '1',
                            name: 'Ethereum',
                            shortName: 'ETH',
                            logoUrl:
                                'https://onchainos.bnbstatic.com/image/admin_mgs_image_upload/20250228/b549e557-76ca-4873-88ef-3a107cf2c9fe.png',
                        },
                    ],
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getTransactionSupportedChains').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetTransactionSupportedChainsResponse>)
            );
            const response = await client.getTransactionSupportedChains();
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute getTransactionSupportedChains() successfully with optional parameters', async () => {
            const params: GetTransactionSupportedChainsRequest = {
                recvWindow: 5000,
                nonce: 'unique-nonce-string',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: [
                        {
                            binanceChainId: '1',
                            name: 'Ethereum',
                            shortName: 'ETH',
                            logoUrl:
                                'https://onchainos.bnbstatic.com/image/admin_mgs_image_upload/20250228/b549e557-76ca-4873-88ef-3a107cf2c9fe.png',
                        },
                    ],
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'getTransactionSupportedChains').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<GetTransactionSupportedChainsResponse>)
            );
            const response = await client.getTransactionSupportedChains(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw an error when server is returning an error', async () => {
            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest
                .spyOn(client, 'getTransactionSupportedChains')
                .mockRejectedValueOnce(mockError);
            await expect(client.getTransactionSupportedChains()).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });

    describe('simulateTransactions()', () => {
        it('should execute simulateTransactions() successfully with required parameters only', async () => {
            const params: SimulateTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        status: 'SUCCESS',
                        failReason: 'execution reverted: ERC20InsufficientBalance',
                        balanceChanges: [
                            {
                                contractAddress: '0xdAC17F958D2ee523a2206206994597C13D831ec7',
                                tokenType: 'ERC20',
                                change: '-1000000',
                                owner: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                            },
                        ],
                        allowanceChanges: [
                            {
                                tokenAddress: '0xdAC17F958D2ee523a2206206994597C13D831ec7',
                                owner: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                                spender: '0x1111111254EEB25477B68fb85Ed929f73A960582',
                                preAmount: '0',
                                postAmount: '1000000',
                            },
                        ],
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'simulateTransactions').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<SimulateTransactionsResponse>)
            );
            const response = await client.simulateTransactions(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should execute simulateTransactions() successfully with optional parameters', async () => {
            const params: SimulateTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
                recvWindow: 5000,
                nonce: 'unique-nonce-string',
                evmTx: {} as SimulateTransactionsRequestEvmTx,
                solTx: {} as SimulateTransactionsRequestSolTx,
            };

            mockResponse = JSONParse(
                JSONStringify({
                    code: 0,
                    msg: 'success',
                    data: {
                        status: 'SUCCESS',
                        failReason: 'execution reverted: ERC20InsufficientBalance',
                        balanceChanges: [
                            {
                                contractAddress: '0xdAC17F958D2ee523a2206206994597C13D831ec7',
                                tokenType: 'ERC20',
                                change: '-1000000',
                                owner: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                            },
                        ],
                        allowanceChanges: [
                            {
                                tokenAddress: '0xdAC17F958D2ee523a2206206994597C13D831ec7',
                                owner: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045',
                                spender: '0x1111111254EEB25477B68fb85Ed929f73A960582',
                                preAmount: '0',
                                postAmount: '1000000',
                            },
                        ],
                    },
                    timestamp: 1748601600000,
                    success: true,
                })
            );

            const spy = jest.spyOn(client, 'simulateTransactions').mockReturnValue(
                Promise.resolve({
                    data: () => Promise.resolve(mockResponse),
                    status: 200,
                    headers: {},
                    rateLimits: [],
                } as RestApiResponse<SimulateTransactionsResponse>)
            );
            const response = await client.simulateTransactions(params);
            expect(response).toBeDefined();
            await expect(response.data()).resolves.toBe(mockResponse);
            spy.mockRestore();
        });

        it('should throw RequiredError when binanceChainId is missing', async () => {
            const _params: SimulateTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
            };
            const params = Object.assign({ ..._params });
            delete params?.binanceChainId;

            await expect(client.simulateTransactions(params)).rejects.toThrow(
                'Required parameter binanceChainId was null or undefined when calling simulateTransactions.'
            );
        });

        it('should throw an error when server is returning an error', async () => {
            const params: SimulateTransactionsRequest = {
                binanceChainId: 'binanceChainId_example',
            };

            const errorResponse = {
                code: -1111,
                msg: 'Server Error',
            };

            const mockError = new Error('ResponseError') as Error & {
                response?: { status: number; data: unknown };
            };
            mockError.response = { status: 400, data: errorResponse };
            const spy = jest.spyOn(client, 'simulateTransactions').mockRejectedValueOnce(mockError);
            await expect(client.simulateTransactions(params)).rejects.toThrow('ResponseError');
            spy.mockRestore();
        });
    });
});
