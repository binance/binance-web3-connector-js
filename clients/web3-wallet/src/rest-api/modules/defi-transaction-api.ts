/**
 * Binance Web3 API
 *
 * Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import {
    ConfigurationRestAPI,
    TimeUnit,
    RestApiResponse,
    assertParamExists,
    sendRequest,
    type RequestArgs,
} from '@binance/common';
import type {
    BuildDeFiClaimTransactionResponse,
    BuildDeFiDepositTransactionRequestToken,
    BuildDeFiDepositTransactionResponse,
    BuildDeFiRedeemTransactionRequestToken,
    BuildDeFiRedeemTransactionResponse,
    BuildLpAddTransactionRequestTokenListInner,
    BuildLpAddTransactionResponse,
    BuildLpRemoveTransactionResponse,
    CalculateLpAddPairedAmountsResponse,
} from '../types';

/**
 * DefiTransactionApi - axios parameter creator
 */
const DefiTransactionApiAxiosParamCreator = function (configuration: ConfigurationRestAPI) {
    return {
        /**
         * Build the unsigned claim calldata for one of four claim types — see `DefiClaimType` for which companion fields each type requires **and which protocol constraints apply** (e.g. `REWARD_PROTOCOL` is not supported for LP protocols; PancakeSwap V3 farming uses `LP_FEE`, not `REWARD_INVESTMENT`). Returns an ordered `dataList` (typically `[CLAIM]`).
         **`binanceChainId` resolution rules**:
         * - Normally resolved from `investmentId`; the call always executes on that chain.
         * - For claim types other than `REWARD_PROTOCOL`, any client-supplied `binanceChainId` is
         **silently ignored** — passing a different chainId will not redirect the claim.
         *
         * - **Exception**: `REWARD_PROTOCOL` with no `investmentId` — the client MUST pass
         * `binanceChainId` (it is the only chain signal).
         *
         * `tokenAddressList` optionally narrows the claim scope. Set `simulate=true` to also receive `preview`.
         *
         * @summary Build DeFi Claim Transaction
         * @param {string} address User wallet address (the signer / sender).
         * @param {BuildDeFiClaimTransactionClaimTypeEnum} claimType Claim routing type. Determines which companion fields are required: - `REWARD_PROTOCOL`: protocol-level rewards (e.g. Aave incentive, Radiant) — requires `defiProtocolId` + `binanceChainId` - `REWARD_INVESTMENT`: per-investment rewards — requires `investmentId` - `LP_FEE`: Uniswap V3/V4 LP fees — requires `investmentId` + `nftId` - `REDEMPTION`: redeem principal (Lista / Aster / Solv etc.) — requires `investmentId` + `redemptionId`  **Protocol constraints** — not every `claimType` is valid for every protocol; an invalid combination is rejected (`40453` or `40454`): - `REWARD_PROTOCOL` is **not** supported for LP (liquidity-pool) protocols in this release. - `REWARD_INVESTMENT` is not supported for PancakeSwap V3 farming positions; use `LP_FEE`   to claim farming incentives there. - `LP_FEE` applies only to concentrated-liquidity (V3/V4) LP positions. - `REDEMPTION` applies only to protocols with a redemption / lock-up flow (Lista, Aster, Solv).  The valid `claimType`s for a specific position are returned in that position\'s `positionDetail.claimTypes` (see the position list) — branch on that rather than hard-coding the mapping. Note: error messages may surface the protocol id with a chain prefix (e.g. `bsc_pancakeswap4`); the canonical id in API fields is the un-prefixed form (e.g. `pancakeswap4`).
         * @param {number | bigint} [recvWindow] Allowed time deviation in milliseconds (default: 5000, max: 60000).
         * @param {string} [nonce] Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
         * @param {string} [binanceChainId] Binance chain ID. For `REWARD_PROTOCOL` (which does not use `investmentId`) this is the only chain signal and is required. For all other claim types the chain is resolved from `investmentId` and any client-supplied value is silently ignored — the call still executes on the `investmentId` chain, so a different chainId does not redirect the claim.
         * @param {string} [investmentId] Required for `REWARD_INVESTMENT`, `LP_FEE`, and `REDEMPTION`. Not used for `REWARD_PROTOCOL` (which keys off `defiProtocolId`).
         * @param {string} [defiProtocolId] Required for `REWARD_PROTOCOL` (e.g. `pancakeswap3`).
         * @param {string} [nftId] Required for `LP_FEE`. The NFT token ID as a decimal string (e.g. `\"7225901\"` or `\"#7225901\"`). The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
         * @param {string} [redemptionId] Required for `REDEMPTION`. Maps to the `redemptionId` returned in the position list (the on-line position index).
         * @param {Array<string>} [tokenAddressList] Optional token address filter to narrow the claim scope.
         * @param {boolean} [simulate] When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
         *
         * @throws {RequiredError}
         */
        buildDeFiClaimTransaction: async (
            address: string,
            claimType: BuildDeFiClaimTransactionClaimTypeEnum,
            recvWindow?: number | bigint,
            nonce?: string,
            binanceChainId?: string,
            investmentId?: string,
            defiProtocolId?: string,
            nftId?: string,
            redemptionId?: string,
            tokenAddressList?: Array<string>,
            simulate?: boolean
        ): Promise<RequestArgs> => {
            // verify required parameter 'address' is not null or undefined
            assertParamExists('buildDeFiClaimTransaction', 'address', address);
            // verify required parameter 'claimType' is not null or undefined
            assertParamExists('buildDeFiClaimTransaction', 'claimType', claimType);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (recvWindow !== undefined && recvWindow !== null) {
                localVarHeaderParameter['recvWindow'] = recvWindow;
            }
            if (nonce !== undefined && nonce !== null) {
                localVarHeaderParameter['nonce'] = nonce;
            }

            if (address !== undefined && address !== null) {
                localVarBodyParameter['address'] = address;
            }

            if (claimType !== undefined && claimType !== null) {
                localVarBodyParameter['claimType'] = claimType;
            }

            if (binanceChainId !== undefined && binanceChainId !== null) {
                localVarBodyParameter['binanceChainId'] = binanceChainId;
            }

            if (investmentId !== undefined && investmentId !== null) {
                localVarBodyParameter['investmentId'] = investmentId;
            }

            if (defiProtocolId !== undefined && defiProtocolId !== null) {
                localVarBodyParameter['defiProtocolId'] = defiProtocolId;
            }

            if (nftId !== undefined && nftId !== null) {
                localVarBodyParameter['nftId'] = nftId;
            }

            if (redemptionId !== undefined && redemptionId !== null) {
                localVarBodyParameter['redemptionId'] = redemptionId;
            }

            if (tokenAddressList) {
                localVarBodyParameter['tokenAddressList'] = tokenAddressList;
            }

            if (simulate !== undefined && simulate !== null) {
                localVarBodyParameter['simulate'] = simulate;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/api/v1/defi/transaction/claim',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Build the unsigned transaction calldata for a DeFi deposit / stake. Returns an ordered `dataList` (typically `[APPROVE, DEPOSIT]`, or `[DEPOSIT]` when the allowance is already sufficient) for the caller to sign and broadcast.
         * The caller does NOT pass `binanceChainId` — the service resolves it from `investmentId`. Set `simulate=true` to also receive a `preview` (projected balance change, estimated gas, and lending health-factor change) without broadcasting.
         *
         * @summary Build DeFi Deposit Transaction
         * @param {string} address User wallet address (the signer / sender).
         * @param {string} investmentId Investment product ID (chain-agnostic). The service resolves the chain from this ID.
         * @param {BuildDeFiDepositTransactionRequestToken} token
         * @param {number | bigint} [recvWindow] Allowed time deviation in milliseconds (default: 5000, max: 60000).
         * @param {string} [nonce] Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
         * @param {boolean} [simulate] When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
         *
         * @throws {RequiredError}
         */
        buildDeFiDepositTransaction: async (
            address: string,
            investmentId: string,
            token: BuildDeFiDepositTransactionRequestToken,
            recvWindow?: number | bigint,
            nonce?: string,
            simulate?: boolean
        ): Promise<RequestArgs> => {
            // verify required parameter 'address' is not null or undefined
            assertParamExists('buildDeFiDepositTransaction', 'address', address);
            // verify required parameter 'investmentId' is not null or undefined
            assertParamExists('buildDeFiDepositTransaction', 'investmentId', investmentId);
            // verify required parameter 'token' is not null or undefined
            assertParamExists('buildDeFiDepositTransaction', 'token', token);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (recvWindow !== undefined && recvWindow !== null) {
                localVarHeaderParameter['recvWindow'] = recvWindow;
            }
            if (nonce !== undefined && nonce !== null) {
                localVarHeaderParameter['nonce'] = nonce;
            }

            if (address !== undefined && address !== null) {
                localVarBodyParameter['address'] = address;
            }

            if (investmentId !== undefined && investmentId !== null) {
                localVarBodyParameter['investmentId'] = investmentId;
            }

            if (token !== undefined && token !== null) {
                localVarBodyParameter['token'] = token;
            }

            if (simulate !== undefined && simulate !== null) {
                localVarBodyParameter['simulate'] = simulate;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/api/v1/defi/transaction/deposit',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Build the unsigned transaction calldata for a DeFi redeem / withdraw. Returns an ordered `dataList` (typically `[REDEEM]` or `[APPROVE, REDEEM]`).
         **Redeem amount is decided by exactly one of `ratio` or `token`** (mutually exclusive): - `ratio` omitted / blank → **exact-amount redeem**: the amount comes from
         * `token` (`tokenAddress` + `amount`); the position is *not* queried. `token` is required
         * in this mode (omitting `token`, or leaving `amount` blank, is rejected with `40001`,
         * `Parameter error`; both `ratio` and `token` absent is also rejected with `40001`).
         * - `ratio` present → **proportional redeem**: must parse as a decimal in `(0, 1]`. The
         * service queries the user's on-chain position under `investmentId` and scales each
         * token's amount by `ratio` (floored to the token's decimals). `ratio="1"` is a full (max)
         * redeem. In this mode `token` is **ignored** — the output tokens and amounts are derived
         * from the position.
         *
         * `slippageBps` is the slippage tolerance in basis points (e.g. `300` = 3%). The caller does NOT pass `binanceChainId` — resolved from `investmentId`. Set `simulate=true` to also receive `preview`.
         * The response also carries `redeemDelayDays` — the redeem waiting period as a `[min, max]` day pair (e.g. `["7","10"]` = 7–10 days, `[]` = instant). The wait starts after the redeem tx is confirmed on-chain. See the `redeemDelayDays` field on `DefiTxResponse`.
         *
         * @summary Build DeFi Redeem Transaction
         * @param {string} address User wallet address (the signer / sender).
         * @param {string} investmentId Investment product ID (chain-agnostic). The service resolves the chain from this ID.
         * @param {number | bigint} [recvWindow] Allowed time deviation in milliseconds (default: 5000, max: 60000).
         * @param {string} [nonce] Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
         * @param {BuildDeFiRedeemTransactionRequestToken} [token]
         * @param {string} [ratio] Redeem ratio, mutually exclusive with `token`. Omitted / blank → exact-amount redeem via `token` (`tokenAddress` + `amount`, position not queried). Present → must parse as a decimal in `(0, 1]`; the service queries the on-chain position under `investmentId` and scales each token amount by `ratio` (floored to decimals). `\"1\"` = full (max) redeem; in this mode `token` is ignored.
         * @param {string} [slippageBps] Slippage tolerance in basis points (e.g. `300` = 3%).
         * @param {boolean} [simulate] When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns an error code instead of a `200` with `preview.success=false`: `40485` when the redeem amount exceeds the current position, `40484` for any other revert (see Error Codes).
         *
         * @throws {RequiredError}
         */
        buildDeFiRedeemTransaction: async (
            address: string,
            investmentId: string,
            recvWindow?: number | bigint,
            nonce?: string,
            token?: BuildDeFiRedeemTransactionRequestToken,
            ratio?: string,
            slippageBps?: string,
            simulate?: boolean
        ): Promise<RequestArgs> => {
            // verify required parameter 'address' is not null or undefined
            assertParamExists('buildDeFiRedeemTransaction', 'address', address);
            // verify required parameter 'investmentId' is not null or undefined
            assertParamExists('buildDeFiRedeemTransaction', 'investmentId', investmentId);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (recvWindow !== undefined && recvWindow !== null) {
                localVarHeaderParameter['recvWindow'] = recvWindow;
            }
            if (nonce !== undefined && nonce !== null) {
                localVarHeaderParameter['nonce'] = nonce;
            }

            if (address !== undefined && address !== null) {
                localVarBodyParameter['address'] = address;
            }

            if (investmentId !== undefined && investmentId !== null) {
                localVarBodyParameter['investmentId'] = investmentId;
            }

            if (token !== undefined && token !== null) {
                localVarBodyParameter['token'] = token;
            }

            if (ratio !== undefined && ratio !== null) {
                localVarBodyParameter['ratio'] = ratio;
            }

            if (slippageBps !== undefined && slippageBps !== null) {
                localVarBodyParameter['slippageBps'] = slippageBps;
            }

            if (simulate !== undefined && simulate !== null) {
                localVarBodyParameter['simulate'] = simulate;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/api/v1/defi/transaction/redeem',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Build the unsigned transaction calldata for adding liquidity. `tokenList` supports multiple tokens. Returns an ordered `dataList` (typically `[APPROVE, LP_ADD]`).
         **Tick range source (provide exactly one group)**:
         * 1. `nftId` — append to an existing LP position (reuses its range).
         * 2. `priceRange` — percentage band (e.g. `"5"` = ±5%) for a new position.
         * 3. `tickLower` + `tickUpper` — explicit raw `int24` pair for a new position.
         **Rules**:
         * - If more than one group is supplied, only the highest-priority one above is used and the
         * others are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).
         *
         * - If none is supplied, the request is rejected (`40453`).
         * - `tickLower` / `tickUpper` are raw `int24` values and MUST be aligned to the pool's
         * `tickSpacing`, otherwise the request is rejected (`40453`).
         *
         * - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
         *
         * @summary Build LP Add Transaction
         * @param {string} address User wallet address (the signer / sender).
         * @param {string} investmentId LP investment product ID (chain-agnostic).
         * @param {Array<BuildLpAddTransactionRequestTokenListInner>} tokenList Tokens to supply. Two tokens for a standard LP add.
         * @param {number | bigint} [recvWindow] Allowed time deviation in milliseconds (default: 5000, max: 60000).
         * @param {string} [nonce] Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
         * @param {string} [tickLower] Lower tick bound (raw `int24`) for a new position. MUST be aligned to the pool\'s `tickSpacing`. Pick exactly one tick-range source: `tickLower`+`tickUpper`, `priceRange`, or `nftId` (to append to an existing position). If several are supplied, the lowest-priority ones are ignored (priority: `nftId` > `priceRange` > explicit tick pair).
         * @param {string} [tickUpper] Upper tick bound (raw `int24`) for a new position. MUST be aligned to the pool\'s `tickSpacing`. See `tickLower` for the pick-exactly-one rule.
         * @param {string} [priceRange] Price range as a percentage (e.g. `\"5\"` = ±5%) for a new position. Alternative to `tickLower`+`tickUpper`; see `tickLower` for the pick-exactly-one rule.
         * @param {string} [nftId] LP NFT token ID, decimal string (e.g. `\"7225901\"` or `\"#7225901\"`). Required when appending to an existing position (reuses its range); omit for a new position. Highest-priority tick-range source — see `tickLower` for the pick-exactly-one rule. The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
         * @param {string} [slippageBps] Slippage tolerance for the LP leg, in basis points (e.g. `300` = 3%).
         * @param {boolean} [simulate] When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
         *
         * @throws {RequiredError}
         */
        buildLpAddTransaction: async (
            address: string,
            investmentId: string,
            tokenList: Array<BuildLpAddTransactionRequestTokenListInner>,
            recvWindow?: number | bigint,
            nonce?: string,
            tickLower?: string,
            tickUpper?: string,
            priceRange?: string,
            nftId?: string,
            slippageBps?: string,
            simulate?: boolean
        ): Promise<RequestArgs> => {
            // verify required parameter 'address' is not null or undefined
            assertParamExists('buildLpAddTransaction', 'address', address);
            // verify required parameter 'investmentId' is not null or undefined
            assertParamExists('buildLpAddTransaction', 'investmentId', investmentId);
            // verify required parameter 'tokenList' is not null or undefined
            assertParamExists('buildLpAddTransaction', 'tokenList', tokenList);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (recvWindow !== undefined && recvWindow !== null) {
                localVarHeaderParameter['recvWindow'] = recvWindow;
            }
            if (nonce !== undefined && nonce !== null) {
                localVarHeaderParameter['nonce'] = nonce;
            }

            if (address !== undefined && address !== null) {
                localVarBodyParameter['address'] = address;
            }

            if (investmentId !== undefined && investmentId !== null) {
                localVarBodyParameter['investmentId'] = investmentId;
            }

            if (tokenList) {
                localVarBodyParameter['tokenList'] = tokenList;
            }

            if (tickLower !== undefined && tickLower !== null) {
                localVarBodyParameter['tickLower'] = tickLower;
            }

            if (tickUpper !== undefined && tickUpper !== null) {
                localVarBodyParameter['tickUpper'] = tickUpper;
            }

            if (priceRange !== undefined && priceRange !== null) {
                localVarBodyParameter['priceRange'] = priceRange;
            }

            if (nftId !== undefined && nftId !== null) {
                localVarBodyParameter['nftId'] = nftId;
            }

            if (slippageBps !== undefined && slippageBps !== null) {
                localVarBodyParameter['slippageBps'] = slippageBps;
            }

            if (simulate !== undefined && simulate !== null) {
                localVarBodyParameter['simulate'] = simulate;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/api/v1/defi/transaction/lp-add',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Build the unsigned transaction calldata for removing liquidity. Removal is by `nftId` + `ratio` (range `(0, 1]`) — no `tokenList` is needed; the per-token amounts are derived from the on-chain position. Returns an ordered `dataList` (typically `[LP_REMOVE]`).
         * `slippageBps` is the LP-leg slippage tolerance in basis points. The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
         *
         * @summary Build LP Remove Transaction
         * @param {string} address User wallet address (the signer / sender).
         * @param {string} investmentId LP investment product ID (chain-agnostic).
         * @param {string} nftId LP NFT token ID identifying the position to remove, decimal string (e.g. `\"7225901\"` or `\"#7225901\"`). The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
         * @param {string} ratio Removal ratio in the range `(0, 1]` as a decimal string (e.g. `\"0.04\"`, `\"1\"`).
         * @param {number | bigint} [recvWindow] Allowed time deviation in milliseconds (default: 5000, max: 60000).
         * @param {string} [nonce] Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
         * @param {string} [slippageBps] Slippage tolerance for the LP leg, in basis points (e.g. `300` = 3%).
         * @param {boolean} [simulate] When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
         *
         * @throws {RequiredError}
         */
        buildLpRemoveTransaction: async (
            address: string,
            investmentId: string,
            nftId: string,
            ratio: string,
            recvWindow?: number | bigint,
            nonce?: string,
            slippageBps?: string,
            simulate?: boolean
        ): Promise<RequestArgs> => {
            // verify required parameter 'address' is not null or undefined
            assertParamExists('buildLpRemoveTransaction', 'address', address);
            // verify required parameter 'investmentId' is not null or undefined
            assertParamExists('buildLpRemoveTransaction', 'investmentId', investmentId);
            // verify required parameter 'nftId' is not null or undefined
            assertParamExists('buildLpRemoveTransaction', 'nftId', nftId);
            // verify required parameter 'ratio' is not null or undefined
            assertParamExists('buildLpRemoveTransaction', 'ratio', ratio);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (recvWindow !== undefined && recvWindow !== null) {
                localVarHeaderParameter['recvWindow'] = recvWindow;
            }
            if (nonce !== undefined && nonce !== null) {
                localVarHeaderParameter['nonce'] = nonce;
            }

            if (address !== undefined && address !== null) {
                localVarBodyParameter['address'] = address;
            }

            if (investmentId !== undefined && investmentId !== null) {
                localVarBodyParameter['investmentId'] = investmentId;
            }

            if (nftId !== undefined && nftId !== null) {
                localVarBodyParameter['nftId'] = nftId;
            }

            if (ratio !== undefined && ratio !== null) {
                localVarBodyParameter['ratio'] = ratio;
            }

            if (slippageBps !== undefined && slippageBps !== null) {
                localVarBodyParameter['slippageBps'] = slippageBps;
            }

            if (simulate !== undefined && simulate !== null) {
                localVarBodyParameter['simulate'] = simulate;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/api/v1/defi/transaction/lp-remove',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
        /**
         * Given a single input token, calculate the paired token amount needed for an LP add. This is a **pure computation endpoint** — it does not touch the chain and does not deduct any fee. Use it before `/transaction/lp-add` to size the paired token when only one side is supplied.
         * Tick range source rules are the same as `/lp-add` — provide exactly one group: `nftId` (existing position), `priceRange`, or `tickLower`+`tickUpper`.
         * - If more than one group is supplied, only the highest-priority one is used and the others
         * are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).
         *
         * - If none is supplied, the request is rejected.
         * - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
         *
         * @summary Calculate LP Add Paired Amounts
         * @param {string} address User wallet address.
         * @param {string} investmentId LP investment product ID (chain-agnostic).
         * @param {BuildDeFiDepositTransactionRequestToken} inputToken
         * @param {number | bigint} [recvWindow] Allowed time deviation in milliseconds (default: 5000, max: 60000).
         * @param {string} [nonce] Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
         * @param {string} [tickLower] Lower tick bound (raw `int24`), aligned to the pool\'s `tickSpacing`.
         * @param {string} [tickUpper] Upper tick bound (raw `int24`), aligned to the pool\'s `tickSpacing`.
         * @param {string} [priceRange] Price range as a percentage (e.g. `\"5\"` = ±5%).
         * @param {string} [nftId] Required when calculating for an existing LP position.
         *
         * @throws {RequiredError}
         */
        calculateLpAddPairedAmounts: async (
            address: string,
            investmentId: string,
            inputToken: BuildDeFiDepositTransactionRequestToken,
            recvWindow?: number | bigint,
            nonce?: string,
            tickLower?: string,
            tickUpper?: string,
            priceRange?: string,
            nftId?: string
        ): Promise<RequestArgs> => {
            // verify required parameter 'address' is not null or undefined
            assertParamExists('calculateLpAddPairedAmounts', 'address', address);
            // verify required parameter 'investmentId' is not null or undefined
            assertParamExists('calculateLpAddPairedAmounts', 'investmentId', investmentId);
            // verify required parameter 'inputToken' is not null or undefined
            assertParamExists('calculateLpAddPairedAmounts', 'inputToken', inputToken);

            const localVarQueryParameter: Record<string, unknown> = {};
            const localVarBodyParameter: Record<string, unknown> = {};
            const localVarHeaderParameter: Record<string, unknown> = {};

            if (recvWindow !== undefined && recvWindow !== null) {
                localVarHeaderParameter['recvWindow'] = recvWindow;
            }
            if (nonce !== undefined && nonce !== null) {
                localVarHeaderParameter['nonce'] = nonce;
            }

            if (address !== undefined && address !== null) {
                localVarBodyParameter['address'] = address;
            }

            if (investmentId !== undefined && investmentId !== null) {
                localVarBodyParameter['investmentId'] = investmentId;
            }

            if (inputToken !== undefined && inputToken !== null) {
                localVarBodyParameter['inputToken'] = inputToken;
            }

            if (tickLower !== undefined && tickLower !== null) {
                localVarBodyParameter['tickLower'] = tickLower;
            }

            if (tickUpper !== undefined && tickUpper !== null) {
                localVarBodyParameter['tickUpper'] = tickUpper;
            }

            if (priceRange !== undefined && priceRange !== null) {
                localVarBodyParameter['priceRange'] = priceRange;
            }

            if (nftId !== undefined && nftId !== null) {
                localVarBodyParameter['nftId'] = nftId;
            }

            let _timeUnit: TimeUnit | undefined;
            if ('timeUnit' in configuration) _timeUnit = configuration.timeUnit as TimeUnit;

            return {
                endpoint: '/api/v1/defi/transaction/lp-add/calculate',
                method: 'POST',
                queryParams: localVarQueryParameter,
                bodyParams: localVarBodyParameter,
                headerParams: localVarHeaderParameter,
                timeUnit: _timeUnit,
            };
        },
    };
};

/**
 * DefiTransactionApi - interface
 * @interface DefiTransactionApi
 */
export interface DefiTransactionApiInterface {
    /**
     * Build the unsigned claim calldata for one of four claim types — see `DefiClaimType` for which companion fields each type requires **and which protocol constraints apply** (e.g. `REWARD_PROTOCOL` is not supported for LP protocols; PancakeSwap V3 farming uses `LP_FEE`, not `REWARD_INVESTMENT`). Returns an ordered `dataList` (typically `[CLAIM]`).
     **`binanceChainId` resolution rules**:
     * - Normally resolved from `investmentId`; the call always executes on that chain.
     * - For claim types other than `REWARD_PROTOCOL`, any client-supplied `binanceChainId` is
     **silently ignored** — passing a different chainId will not redirect the claim.
     *
     * - **Exception**: `REWARD_PROTOCOL` with no `investmentId` — the client MUST pass
     * `binanceChainId` (it is the only chain signal).
     *
     * `tokenAddressList` optionally narrows the claim scope. Set `simulate=true` to also receive `preview`.
     *
     * @summary Build DeFi Claim Transaction
     * @param {BuildDeFiClaimTransactionRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApiInterface
     */
    buildDeFiClaimTransaction(
        requestParameters: BuildDeFiClaimTransactionRequest
    ): Promise<RestApiResponse<BuildDeFiClaimTransactionResponse>>;
    /**
     * Build the unsigned transaction calldata for a DeFi deposit / stake. Returns an ordered `dataList` (typically `[APPROVE, DEPOSIT]`, or `[DEPOSIT]` when the allowance is already sufficient) for the caller to sign and broadcast.
     * The caller does NOT pass `binanceChainId` — the service resolves it from `investmentId`. Set `simulate=true` to also receive a `preview` (projected balance change, estimated gas, and lending health-factor change) without broadcasting.
     *
     * @summary Build DeFi Deposit Transaction
     * @param {BuildDeFiDepositTransactionRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApiInterface
     */
    buildDeFiDepositTransaction(
        requestParameters: BuildDeFiDepositTransactionRequest
    ): Promise<RestApiResponse<BuildDeFiDepositTransactionResponse>>;
    /**
     * Build the unsigned transaction calldata for a DeFi redeem / withdraw. Returns an ordered `dataList` (typically `[REDEEM]` or `[APPROVE, REDEEM]`).
     **Redeem amount is decided by exactly one of `ratio` or `token`** (mutually exclusive): - `ratio` omitted / blank → **exact-amount redeem**: the amount comes from
     * `token` (`tokenAddress` + `amount`); the position is *not* queried. `token` is required
     * in this mode (omitting `token`, or leaving `amount` blank, is rejected with `40001`,
     * `Parameter error`; both `ratio` and `token` absent is also rejected with `40001`).
     * - `ratio` present → **proportional redeem**: must parse as a decimal in `(0, 1]`. The
     * service queries the user's on-chain position under `investmentId` and scales each
     * token's amount by `ratio` (floored to the token's decimals). `ratio="1"` is a full (max)
     * redeem. In this mode `token` is **ignored** — the output tokens and amounts are derived
     * from the position.
     *
     * `slippageBps` is the slippage tolerance in basis points (e.g. `300` = 3%). The caller does NOT pass `binanceChainId` — resolved from `investmentId`. Set `simulate=true` to also receive `preview`.
     * The response also carries `redeemDelayDays` — the redeem waiting period as a `[min, max]` day pair (e.g. `["7","10"]` = 7–10 days, `[]` = instant). The wait starts after the redeem tx is confirmed on-chain. See the `redeemDelayDays` field on `DefiTxResponse`.
     *
     * @summary Build DeFi Redeem Transaction
     * @param {BuildDeFiRedeemTransactionRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApiInterface
     */
    buildDeFiRedeemTransaction(
        requestParameters: BuildDeFiRedeemTransactionRequest
    ): Promise<RestApiResponse<BuildDeFiRedeemTransactionResponse>>;
    /**
     * Build the unsigned transaction calldata for adding liquidity. `tokenList` supports multiple tokens. Returns an ordered `dataList` (typically `[APPROVE, LP_ADD]`).
     **Tick range source (provide exactly one group)**:
     * 1. `nftId` — append to an existing LP position (reuses its range).
     * 2. `priceRange` — percentage band (e.g. `"5"` = ±5%) for a new position.
     * 3. `tickLower` + `tickUpper` — explicit raw `int24` pair for a new position.
     **Rules**:
     * - If more than one group is supplied, only the highest-priority one above is used and the
     * others are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).
     *
     * - If none is supplied, the request is rejected (`40453`).
     * - `tickLower` / `tickUpper` are raw `int24` values and MUST be aligned to the pool's
     * `tickSpacing`, otherwise the request is rejected (`40453`).
     *
     * - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
     *
     * @summary Build LP Add Transaction
     * @param {BuildLpAddTransactionRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApiInterface
     */
    buildLpAddTransaction(
        requestParameters: BuildLpAddTransactionRequest
    ): Promise<RestApiResponse<BuildLpAddTransactionResponse>>;
    /**
     * Build the unsigned transaction calldata for removing liquidity. Removal is by `nftId` + `ratio` (range `(0, 1]`) — no `tokenList` is needed; the per-token amounts are derived from the on-chain position. Returns an ordered `dataList` (typically `[LP_REMOVE]`).
     * `slippageBps` is the LP-leg slippage tolerance in basis points. The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
     *
     * @summary Build LP Remove Transaction
     * @param {BuildLpRemoveTransactionRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApiInterface
     */
    buildLpRemoveTransaction(
        requestParameters: BuildLpRemoveTransactionRequest
    ): Promise<RestApiResponse<BuildLpRemoveTransactionResponse>>;
    /**
     * Given a single input token, calculate the paired token amount needed for an LP add. This is a **pure computation endpoint** — it does not touch the chain and does not deduct any fee. Use it before `/transaction/lp-add` to size the paired token when only one side is supplied.
     * Tick range source rules are the same as `/lp-add` — provide exactly one group: `nftId` (existing position), `priceRange`, or `tickLower`+`tickUpper`.
     * - If more than one group is supplied, only the highest-priority one is used and the others
     * are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).
     *
     * - If none is supplied, the request is rejected.
     * - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
     *
     * @summary Calculate LP Add Paired Amounts
     * @param {CalculateLpAddPairedAmountsRequest} requestParameters Request parameters.
     *
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApiInterface
     */
    calculateLpAddPairedAmounts(
        requestParameters: CalculateLpAddPairedAmountsRequest
    ): Promise<RestApiResponse<CalculateLpAddPairedAmountsResponse>>;
}

/**
 * Request parameters for buildDeFiClaimTransaction operation in DefiTransactionApi.
 * @interface BuildDeFiClaimTransactionRequest
 */
export interface BuildDeFiClaimTransactionRequest {
    /**
     * User wallet address (the signer / sender).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly address: string;

    /**
     * Claim routing type. Determines which companion fields are required: - `REWARD_PROTOCOL`: protocol-level rewards (e.g. Aave incentive, Radiant) — requires `defiProtocolId` + `binanceChainId` - `REWARD_INVESTMENT`: per-investment rewards — requires `investmentId` - `LP_FEE`: Uniswap V3/V4 LP fees — requires `investmentId` + `nftId` - `REDEMPTION`: redeem principal (Lista / Aster / Solv etc.) — requires `investmentId` + `redemptionId`  **Protocol constraints** — not every `claimType` is valid for every protocol; an invalid combination is rejected (`40453` or `40454`): - `REWARD_PROTOCOL` is **not** supported for LP (liquidity-pool) protocols in this release. - `REWARD_INVESTMENT` is not supported for PancakeSwap V3 farming positions; use `LP_FEE`   to claim farming incentives there. - `LP_FEE` applies only to concentrated-liquidity (V3/V4) LP positions. - `REDEMPTION` applies only to protocols with a redemption / lock-up flow (Lista, Aster, Solv).  The valid `claimType`s for a specific position are returned in that position\'s `positionDetail.claimTypes` (see the position list) — branch on that rather than hard-coding the mapping. Note: error messages may surface the protocol id with a chain prefix (e.g. `bsc_pancakeswap4`); the canonical id in API fields is the un-prefixed form (e.g. `pancakeswap4`).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly claimType: BuildDeFiClaimTransactionClaimTypeEnum;

    /**
     * Allowed time deviation in milliseconds (default: 5000, max: 60000).
     * @type {number | bigint}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly recvWindow?: number | bigint;

    /**
     * Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly nonce?: string;

    /**
     * Binance chain ID. For `REWARD_PROTOCOL` (which does not use `investmentId`) this is the only chain signal and is required. For all other claim types the chain is resolved from `investmentId` and any client-supplied value is silently ignored — the call still executes on the `investmentId` chain, so a different chainId does not redirect the claim.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly binanceChainId?: string;

    /**
     * Required for `REWARD_INVESTMENT`, `LP_FEE`, and `REDEMPTION`. Not used for `REWARD_PROTOCOL` (which keys off `defiProtocolId`).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly investmentId?: string;

    /**
     * Required for `REWARD_PROTOCOL` (e.g. `pancakeswap3`).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly defiProtocolId?: string;

    /**
     * Required for `LP_FEE`. The NFT token ID as a decimal string (e.g. `\"7225901\"` or `\"#7225901\"`). The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly nftId?: string;

    /**
     * Required for `REDEMPTION`. Maps to the `redemptionId` returned in the position list (the on-line position index).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly redemptionId?: string;

    /**
     * Optional token address filter to narrow the claim scope.
     * @type {Array<string>}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly tokenAddressList?: Array<string>;

    /**
     * When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
     * @type {boolean}
     * @memberof DefiTransactionApiBuildDeFiClaimTransaction
     */
    readonly simulate?: boolean;
}

/**
 * Request parameters for buildDeFiDepositTransaction operation in DefiTransactionApi.
 * @interface BuildDeFiDepositTransactionRequest
 */
export interface BuildDeFiDepositTransactionRequest {
    /**
     * User wallet address (the signer / sender).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiDepositTransaction
     */
    readonly address: string;

    /**
     * Investment product ID (chain-agnostic). The service resolves the chain from this ID.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiDepositTransaction
     */
    readonly investmentId: string;

    /**
     *
     * @type {BuildDeFiDepositTransactionRequestToken}
     * @memberof DefiTransactionApiBuildDeFiDepositTransaction
     */
    readonly token: BuildDeFiDepositTransactionRequestToken;

    /**
     * Allowed time deviation in milliseconds (default: 5000, max: 60000).
     * @type {number | bigint}
     * @memberof DefiTransactionApiBuildDeFiDepositTransaction
     */
    readonly recvWindow?: number | bigint;

    /**
     * Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiDepositTransaction
     */
    readonly nonce?: string;

    /**
     * When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
     * @type {boolean}
     * @memberof DefiTransactionApiBuildDeFiDepositTransaction
     */
    readonly simulate?: boolean;
}

/**
 * Request parameters for buildDeFiRedeemTransaction operation in DefiTransactionApi.
 * @interface BuildDeFiRedeemTransactionRequest
 */
export interface BuildDeFiRedeemTransactionRequest {
    /**
     * User wallet address (the signer / sender).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly address: string;

    /**
     * Investment product ID (chain-agnostic). The service resolves the chain from this ID.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly investmentId: string;

    /**
     * Allowed time deviation in milliseconds (default: 5000, max: 60000).
     * @type {number | bigint}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly recvWindow?: number | bigint;

    /**
     * Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly nonce?: string;

    /**
     *
     * @type {BuildDeFiRedeemTransactionRequestToken}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly token?: BuildDeFiRedeemTransactionRequestToken;

    /**
     * Redeem ratio, mutually exclusive with `token`. Omitted / blank → exact-amount redeem via `token` (`tokenAddress` + `amount`, position not queried). Present → must parse as a decimal in `(0, 1]`; the service queries the on-chain position under `investmentId` and scales each token amount by `ratio` (floored to decimals). `\"1\"` = full (max) redeem; in this mode `token` is ignored.
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly ratio?: string;

    /**
     * Slippage tolerance in basis points (e.g. `300` = 3%).
     * @type {string}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly slippageBps?: string;

    /**
     * When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns an error code instead of a `200` with `preview.success=false`: `40485` when the redeem amount exceeds the current position, `40484` for any other revert (see Error Codes).
     * @type {boolean}
     * @memberof DefiTransactionApiBuildDeFiRedeemTransaction
     */
    readonly simulate?: boolean;
}

/**
 * Request parameters for buildLpAddTransaction operation in DefiTransactionApi.
 * @interface BuildLpAddTransactionRequest
 */
export interface BuildLpAddTransactionRequest {
    /**
     * User wallet address (the signer / sender).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly address: string;

    /**
     * LP investment product ID (chain-agnostic).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly investmentId: string;

    /**
     * Tokens to supply. Two tokens for a standard LP add.
     * @type {Array<BuildLpAddTransactionRequestTokenListInner>}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly tokenList: Array<BuildLpAddTransactionRequestTokenListInner>;

    /**
     * Allowed time deviation in milliseconds (default: 5000, max: 60000).
     * @type {number | bigint}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly recvWindow?: number | bigint;

    /**
     * Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly nonce?: string;

    /**
     * Lower tick bound (raw `int24`) for a new position. MUST be aligned to the pool\'s `tickSpacing`. Pick exactly one tick-range source: `tickLower`+`tickUpper`, `priceRange`, or `nftId` (to append to an existing position). If several are supplied, the lowest-priority ones are ignored (priority: `nftId` > `priceRange` > explicit tick pair).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly tickLower?: string;

    /**
     * Upper tick bound (raw `int24`) for a new position. MUST be aligned to the pool\'s `tickSpacing`. See `tickLower` for the pick-exactly-one rule.
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly tickUpper?: string;

    /**
     * Price range as a percentage (e.g. `\"5\"` = ±5%) for a new position. Alternative to `tickLower`+`tickUpper`; see `tickLower` for the pick-exactly-one rule.
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly priceRange?: string;

    /**
     * LP NFT token ID, decimal string (e.g. `\"7225901\"` or `\"#7225901\"`). Required when appending to an existing position (reuses its range); omit for a new position. Highest-priority tick-range source — see `tickLower` for the pick-exactly-one rule. The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly nftId?: string;

    /**
     * Slippage tolerance for the LP leg, in basis points (e.g. `300` = 3%).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly slippageBps?: string;

    /**
     * When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
     * @type {boolean}
     * @memberof DefiTransactionApiBuildLpAddTransaction
     */
    readonly simulate?: boolean;
}

/**
 * Request parameters for buildLpRemoveTransaction operation in DefiTransactionApi.
 * @interface BuildLpRemoveTransactionRequest
 */
export interface BuildLpRemoveTransactionRequest {
    /**
     * User wallet address (the signer / sender).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly address: string;

    /**
     * LP investment product ID (chain-agnostic).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly investmentId: string;

    /**
     * LP NFT token ID identifying the position to remove, decimal string (e.g. `\"7225901\"` or `\"#7225901\"`). The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
     * @type {string}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly nftId: string;

    /**
     * Removal ratio in the range `(0, 1]` as a decimal string (e.g. `\"0.04\"`, `\"1\"`).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly ratio: string;

    /**
     * Allowed time deviation in milliseconds (default: 5000, max: 60000).
     * @type {number | bigint}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly recvWindow?: number | bigint;

    /**
     * Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
     * @type {string}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly nonce?: string;

    /**
     * Slippage tolerance for the LP leg, in basis points (e.g. `300` = 3%).
     * @type {string}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly slippageBps?: string;

    /**
     * When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).
     * @type {boolean}
     * @memberof DefiTransactionApiBuildLpRemoveTransaction
     */
    readonly simulate?: boolean;
}

/**
 * Request parameters for calculateLpAddPairedAmounts operation in DefiTransactionApi.
 * @interface CalculateLpAddPairedAmountsRequest
 */
export interface CalculateLpAddPairedAmountsRequest {
    /**
     * User wallet address.
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly address: string;

    /**
     * LP investment product ID (chain-agnostic).
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly investmentId: string;

    /**
     *
     * @type {BuildDeFiDepositTransactionRequestToken}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly inputToken: BuildDeFiDepositTransactionRequestToken;

    /**
     * Allowed time deviation in milliseconds (default: 5000, max: 60000).
     * @type {number | bigint}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly recvWindow?: number | bigint;

    /**
     * Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly nonce?: string;

    /**
     * Lower tick bound (raw `int24`), aligned to the pool\'s `tickSpacing`.
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly tickLower?: string;

    /**
     * Upper tick bound (raw `int24`), aligned to the pool\'s `tickSpacing`.
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly tickUpper?: string;

    /**
     * Price range as a percentage (e.g. `\"5\"` = ±5%).
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly priceRange?: string;

    /**
     * Required when calculating for an existing LP position.
     * @type {string}
     * @memberof DefiTransactionApiCalculateLpAddPairedAmounts
     */
    readonly nftId?: string;
}

/**
 * DefiTransactionApi - object-oriented interface
 * @class DefiTransactionApi
 */
export class DefiTransactionApi implements DefiTransactionApiInterface {
    private readonly configuration: ConfigurationRestAPI;
    private localVarAxiosParamCreator;

    constructor(configuration: ConfigurationRestAPI) {
        this.configuration = configuration;
        this.localVarAxiosParamCreator = DefiTransactionApiAxiosParamCreator(configuration);
    }

    /**
     * Build the unsigned claim calldata for one of four claim types — see `DefiClaimType` for which companion fields each type requires **and which protocol constraints apply** (e.g. `REWARD_PROTOCOL` is not supported for LP protocols; PancakeSwap V3 farming uses `LP_FEE`, not `REWARD_INVESTMENT`). Returns an ordered `dataList` (typically `[CLAIM]`).
     **`binanceChainId` resolution rules**:
     * - Normally resolved from `investmentId`; the call always executes on that chain.
     * - For claim types other than `REWARD_PROTOCOL`, any client-supplied `binanceChainId` is
     **silently ignored** — passing a different chainId will not redirect the claim.
     *
     * - **Exception**: `REWARD_PROTOCOL` with no `investmentId` — the client MUST pass
     * `binanceChainId` (it is the only chain signal).
     *
     * `tokenAddressList` optionally narrows the claim scope. Set `simulate=true` to also receive `preview`.
     *
     * @summary Build DeFi Claim Transaction
     * @param {BuildDeFiClaimTransactionRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<BuildDeFiClaimTransactionResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApi
     * @see {@link https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/rest-api/defi-transaction#build-de-fi-claim-transaction Binance API Documentation}
     */
    public async buildDeFiClaimTransaction(
        requestParameters: BuildDeFiClaimTransactionRequest
    ): Promise<RestApiResponse<BuildDeFiClaimTransactionResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.buildDeFiClaimTransaction(
            requestParameters?.address,
            requestParameters?.claimType,
            requestParameters?.recvWindow,
            requestParameters?.nonce,
            requestParameters?.binanceChainId,
            requestParameters?.investmentId,
            requestParameters?.defiProtocolId,
            requestParameters?.nftId,
            requestParameters?.redemptionId,
            requestParameters?.tokenAddressList,
            requestParameters?.simulate
        );
        return sendRequest<BuildDeFiClaimTransactionResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Build the unsigned transaction calldata for a DeFi deposit / stake. Returns an ordered `dataList` (typically `[APPROVE, DEPOSIT]`, or `[DEPOSIT]` when the allowance is already sufficient) for the caller to sign and broadcast.
     * The caller does NOT pass `binanceChainId` — the service resolves it from `investmentId`. Set `simulate=true` to also receive a `preview` (projected balance change, estimated gas, and lending health-factor change) without broadcasting.
     *
     * @summary Build DeFi Deposit Transaction
     * @param {BuildDeFiDepositTransactionRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<BuildDeFiDepositTransactionResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApi
     * @see {@link https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/rest-api/defi-transaction#build-de-fi-deposit-transaction Binance API Documentation}
     */
    public async buildDeFiDepositTransaction(
        requestParameters: BuildDeFiDepositTransactionRequest
    ): Promise<RestApiResponse<BuildDeFiDepositTransactionResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.buildDeFiDepositTransaction(
            requestParameters?.address,
            requestParameters?.investmentId,
            requestParameters?.token,
            requestParameters?.recvWindow,
            requestParameters?.nonce,
            requestParameters?.simulate
        );
        return sendRequest<BuildDeFiDepositTransactionResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Build the unsigned transaction calldata for a DeFi redeem / withdraw. Returns an ordered `dataList` (typically `[REDEEM]` or `[APPROVE, REDEEM]`).
     **Redeem amount is decided by exactly one of `ratio` or `token`** (mutually exclusive): - `ratio` omitted / blank → **exact-amount redeem**: the amount comes from
     * `token` (`tokenAddress` + `amount`); the position is *not* queried. `token` is required
     * in this mode (omitting `token`, or leaving `amount` blank, is rejected with `40001`,
     * `Parameter error`; both `ratio` and `token` absent is also rejected with `40001`).
     * - `ratio` present → **proportional redeem**: must parse as a decimal in `(0, 1]`. The
     * service queries the user's on-chain position under `investmentId` and scales each
     * token's amount by `ratio` (floored to the token's decimals). `ratio="1"` is a full (max)
     * redeem. In this mode `token` is **ignored** — the output tokens and amounts are derived
     * from the position.
     *
     * `slippageBps` is the slippage tolerance in basis points (e.g. `300` = 3%). The caller does NOT pass `binanceChainId` — resolved from `investmentId`. Set `simulate=true` to also receive `preview`.
     * The response also carries `redeemDelayDays` — the redeem waiting period as a `[min, max]` day pair (e.g. `["7","10"]` = 7–10 days, `[]` = instant). The wait starts after the redeem tx is confirmed on-chain. See the `redeemDelayDays` field on `DefiTxResponse`.
     *
     * @summary Build DeFi Redeem Transaction
     * @param {BuildDeFiRedeemTransactionRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<BuildDeFiRedeemTransactionResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApi
     * @see {@link https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/rest-api/defi-transaction#build-de-fi-redeem-transaction Binance API Documentation}
     */
    public async buildDeFiRedeemTransaction(
        requestParameters: BuildDeFiRedeemTransactionRequest
    ): Promise<RestApiResponse<BuildDeFiRedeemTransactionResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.buildDeFiRedeemTransaction(
            requestParameters?.address,
            requestParameters?.investmentId,
            requestParameters?.recvWindow,
            requestParameters?.nonce,
            requestParameters?.token,
            requestParameters?.ratio,
            requestParameters?.slippageBps,
            requestParameters?.simulate
        );
        return sendRequest<BuildDeFiRedeemTransactionResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Build the unsigned transaction calldata for adding liquidity. `tokenList` supports multiple tokens. Returns an ordered `dataList` (typically `[APPROVE, LP_ADD]`).
     **Tick range source (provide exactly one group)**:
     * 1. `nftId` — append to an existing LP position (reuses its range).
     * 2. `priceRange` — percentage band (e.g. `"5"` = ±5%) for a new position.
     * 3. `tickLower` + `tickUpper` — explicit raw `int24` pair for a new position.
     **Rules**:
     * - If more than one group is supplied, only the highest-priority one above is used and the
     * others are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).
     *
     * - If none is supplied, the request is rejected (`40453`).
     * - `tickLower` / `tickUpper` are raw `int24` values and MUST be aligned to the pool's
     * `tickSpacing`, otherwise the request is rejected (`40453`).
     *
     * - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
     *
     * @summary Build LP Add Transaction
     * @param {BuildLpAddTransactionRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<BuildLpAddTransactionResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApi
     * @see {@link https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/rest-api/defi-transaction#build-lp-add-transaction Binance API Documentation}
     */
    public async buildLpAddTransaction(
        requestParameters: BuildLpAddTransactionRequest
    ): Promise<RestApiResponse<BuildLpAddTransactionResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.buildLpAddTransaction(
            requestParameters?.address,
            requestParameters?.investmentId,
            requestParameters?.tokenList,
            requestParameters?.recvWindow,
            requestParameters?.nonce,
            requestParameters?.tickLower,
            requestParameters?.tickUpper,
            requestParameters?.priceRange,
            requestParameters?.nftId,
            requestParameters?.slippageBps,
            requestParameters?.simulate
        );
        return sendRequest<BuildLpAddTransactionResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Build the unsigned transaction calldata for removing liquidity. Removal is by `nftId` + `ratio` (range `(0, 1]`) — no `tokenList` is needed; the per-token amounts are derived from the on-chain position. Returns an ordered `dataList` (typically `[LP_REMOVE]`).
     * `slippageBps` is the LP-leg slippage tolerance in basis points. The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
     *
     * @summary Build LP Remove Transaction
     * @param {BuildLpRemoveTransactionRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<BuildLpRemoveTransactionResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApi
     * @see {@link https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/rest-api/defi-transaction#build-lp-remove-transaction Binance API Documentation}
     */
    public async buildLpRemoveTransaction(
        requestParameters: BuildLpRemoveTransactionRequest
    ): Promise<RestApiResponse<BuildLpRemoveTransactionResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.buildLpRemoveTransaction(
            requestParameters?.address,
            requestParameters?.investmentId,
            requestParameters?.nftId,
            requestParameters?.ratio,
            requestParameters?.recvWindow,
            requestParameters?.nonce,
            requestParameters?.slippageBps,
            requestParameters?.simulate
        );
        return sendRequest<BuildLpRemoveTransactionResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }

    /**
     * Given a single input token, calculate the paired token amount needed for an LP add. This is a **pure computation endpoint** — it does not touch the chain and does not deduct any fee. Use it before `/transaction/lp-add` to size the paired token when only one side is supplied.
     * Tick range source rules are the same as `/lp-add` — provide exactly one group: `nftId` (existing position), `priceRange`, or `tickLower`+`tickUpper`.
     * - If more than one group is supplied, only the highest-priority one is used and the others
     * are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).
     *
     * - If none is supplied, the request is rejected.
     * - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.
     *
     * @summary Calculate LP Add Paired Amounts
     * @param {CalculateLpAddPairedAmountsRequest} requestParameters Request parameters.
     * @returns {Promise<RestApiResponse<CalculateLpAddPairedAmountsResponse>>}
     * @throws {RequiredError | ConnectorClientError | UnauthorizedError | ForbiddenError | TooManyRequestsError | RateLimitBanError | ServerError | NotFoundError | NetworkError | BadRequestError}
     * @memberof DefiTransactionApi
     * @see {@link https://web3.binance.com/en/dev-docs/catalog/web3-wallet/api/rest-api/defi-transaction#calculate-lp-add-paired-amounts Binance API Documentation}
     */
    public async calculateLpAddPairedAmounts(
        requestParameters: CalculateLpAddPairedAmountsRequest
    ): Promise<RestApiResponse<CalculateLpAddPairedAmountsResponse>> {
        const localVarAxiosArgs = await this.localVarAxiosParamCreator.calculateLpAddPairedAmounts(
            requestParameters?.address,
            requestParameters?.investmentId,
            requestParameters?.inputToken,
            requestParameters?.recvWindow,
            requestParameters?.nonce,
            requestParameters?.tickLower,
            requestParameters?.tickUpper,
            requestParameters?.priceRange,
            requestParameters?.nftId
        );
        return sendRequest<CalculateLpAddPairedAmountsResponse>(
            this.configuration,
            localVarAxiosArgs.endpoint,
            localVarAxiosArgs.method,
            localVarAxiosArgs.queryParams,
            localVarAxiosArgs.bodyParams,
            localVarAxiosArgs.headerParams,
            localVarAxiosArgs?.timeUnit,
            { isSigned: true }
        );
    }
}

export enum BuildDeFiClaimTransactionClaimTypeEnum {
    REWARD_PROTOCOL = 'REWARD_PROTOCOL',
    REWARD_INVESTMENT = 'REWARD_INVESTMENT',
    LP_FEE = 'LP_FEE',
    REDEMPTION = 'REDEMPTION',
}
