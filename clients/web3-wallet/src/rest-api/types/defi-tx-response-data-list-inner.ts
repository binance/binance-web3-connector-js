/* tslint:disable */

/**
 * Binance Web3 API
 *
 * Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/**
 * A single unsigned transaction to sign and broadcast. The `dataList` is ordered: APPROVE items precede the main action. EVM chains return `to`/`value` (hex) and hex calldata in `data`. **Number-format conventions for this object** — `value` is a `0x`-prefixed hex string; `gasLimit`, `gasPrice`, `maxPriorityFeePerGas`, and `maxFeePerGas` are decimal strings. Mixed hex/decimal within the same object is by design (it mirrors the on-chain transaction fields); always parse gas fields with base-10 and `value` with base-16. **BSC and EIP-1559**: this release is BSC only. BSC is an EIP-1559 chain, so build responses populate `maxFeePerGas` and `maxPriorityFeePerGas`; `gasPrice` is `null` (legacy-only). The nullable markers below are retained so the schema also describes non-1559 chains in later releases — on BSC the 1559 fields are present and the legacy `gasPrice` is `null`. `to` and `value` are non-null for every action item on BSC.
 * @export
 * @interface DefiTxResponseDataListInner
 */
export interface DefiTxResponseDataListInner {
    /**
     * Action kind of this item: - `APPROVE` — ERC-20 approval, must be signed/broadcast and confirmed before the main action - `DEPOSIT` / `REDEEM` / `LP_ADD` / `LP_REMOVE` / `CLAIM` — the main action.   `LP_ADD` / `LP_REMOVE` are the LP add / remove legs (the same build path as   deposit / redeem for liquidity-pool investments; the kind reflects the on-chain action,   not the entry endpoint).
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    callDataType?: DefiTxResponseDataListInnerCallDataTypeEnum;
    /**
     * User wallet address (the signer / sender). EIP-55 checksummed — normalize with `to_checksum_address` before signing if your signer requires it.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    from?: string;
    /**
     * Target contract address. EIP-55 checksummed, same format as `from`. Non-null on BSC for every action item.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    to?: string | null;
    /**
     * Native-asset value as a `0x`-prefixed hex string. Non-null on BSC for action items.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    value?: string | null;
    /**
     * Serialized transaction data, as a `0x`-prefixed hex calldata string.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    data?: string;
    /**
     * Suggested gas limit (decimal string). `null` when estimation fails.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    gasLimit?: string | null;
    /**
     * Legacy gas price, decimal string. `null` on EIP-1559 chains such as BSC (use `maxFeePerGas` / `maxPriorityFeePerGas` instead).
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    gasPrice?: string | null;
    /**
     * EIP-1559 priority fee in wei (decimal string). `null` on non-1559 chains.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    maxPriorityFeePerGas?: string | null;
    /**
     * EIP-1559 max fee per gas in wei (decimal string). `null` on non-1559 chains.
     * @type {string}
     * @memberof DefiTxResponseDataListInner
     */
    maxFeePerGas?: string | null;
}

export const DefiTxResponseDataListInnerCallDataTypeEnum = {
    APPROVE: 'APPROVE',
    DEPOSIT: 'DEPOSIT',
    REDEEM: 'REDEEM',
    LP_ADD: 'LP_ADD',
    LP_REMOVE: 'LP_REMOVE',
    CLAIM: 'CLAIM',
} as const;

export type DefiTxResponseDataListInnerCallDataTypeEnum =
    (typeof DefiTxResponseDataListInnerCallDataTypeEnum)[keyof typeof DefiTxResponseDataListInnerCallDataTypeEnum];
